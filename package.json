{
  "name": "eduslides",
  "version": "1.0.0",
  "scripts": {
    "dashboard": "concurrently \"node server.js\" \"cd frontend && npm start\"",
    "server": "node server.js",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
